name: GORS Tests

on:
  pull_request:
    branches: [main]
  push:
    branches:
      - main
      - codex/implement-frozen-hr5-manual-trading-dashboard
      - fix/persistent-neon-db

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pytest numpy pandas yfinance
      - name: Secret scan
        run: python scripts/secret_scan.py
      - name: Compile production Python
        run: python -m py_compile GORS_APP_PROD/gors_db.py GORS_APP_PROD/pages/Rotation_Signal.py GORS_APP_PROD/pages/History.py GORS_APP_PROD/rotation_history.py
      - name: Run GORS tests
        run: python -m pytest tests/test_gors_engine.py tests/test_phase7_dashboard.py tests/test_etf_rotation_history.py tests/test_rotation_history_page.py -q
